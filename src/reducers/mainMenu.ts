 
const mainMenuReducer = (state = '', action: any) => {
    switch (action.type){
        case '':
            return state;
        default:
            return state;
    }
}

export default mainMenuReducer;